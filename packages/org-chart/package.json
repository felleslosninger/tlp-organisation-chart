{
  "name": "@root/org-chart",
  "version": "0.0.0",
  "description": "Org chart generator",
  "license": "MIT",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "pnpm run clean && tsc -p tsconfig.json && rollup -c --bundleConfigAsCjs",
    "clean": "rimraf dist && rimraf tsc-build",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@rollup/plugin-commonjs": "^25.0.7",
    "@rollup/plugin-node-resolve": "^15.2.3",
    "rimraf": "^5.0.5",
    "rollup-plugin-peer-deps-external": "^2.2.4",
    "rollup-plugin-postcss": "^4.0.2",
    "typescript": "^4.9.5"
  }
}
