{
  "name": "@digdir/organization-chart",
  "version": "0.1.0",
  "description": "Organization chart generator",
  "license": "MIT",
  "types": "dist/types/src/index.d.ts",
  "main": "dist/index.js",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "pnpm run clean && tsc -p tsconfig.json && rollup -c",
    "clean": "rimraf dist && rimraf tsc-build",
    "test": "jest",
    "watch": "chokidar \"src/**/*\" -c \"pnpm run build\""
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@rollup/plugin-commonjs": "^25.0.7",
    "@rollup/plugin-node-resolve": "^15.2.3",
    "postcss": "^8.4.35",
    "rimraf": "^5.0.5",
    "rollup-plugin-copy": "^3.5.0",
    "rollup-plugin-peer-deps-external": "^2.2.4",
    "rollup-plugin-postcss": "^4.0.2",
    "typescript": "^4.9.5"
  }
}
